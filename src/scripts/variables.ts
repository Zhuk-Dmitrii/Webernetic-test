export const variables = {
  // Layout
  modalAuth: document.getElementById('modal-auth') as HTMLDivElement,

  // Buttons
  btnDemo: document.getElementById('btn-demo') as HTMLButtonElement,
  btnClose: document.getElementById('btn-close-modal') as HTMLButtonElement,

  // Form elements
  formSignIn: document.getElementById('form-sign-in') as HTMLFormElement,
  inputLogoSignIn: document.getElementById('login-sign-in') as HTMLInputElement,
  inputPassSignIn: document.getElementById('password-sign-in') as HTMLInputElement,
}
